# SnowCrash

## Objective

We have 15 levels.

For each level we need to find a way to get our flag using the `getflag` command.

## Setup

```
NAME	PROTOCOL	HOST IP		Host Port	Guest Port
SSH		TCP			127.0.0.1	4243		4242
```
